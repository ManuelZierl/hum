# Minimal Lightning Demo

Run a tiny demo consuming `@hum/lightning-client` with the in-memory mock
provider.

## Development

```bash
npm run dev
```

## Build

```bash
npm run build
```

Then open `index.html` in a browser (the dev script already serves it using
`http-server`).
