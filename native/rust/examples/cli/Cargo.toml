[package]
name = "hum-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
hum-matrix-core = { path = "../../crates/core" }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal"] }
futures-util = "0.3"
