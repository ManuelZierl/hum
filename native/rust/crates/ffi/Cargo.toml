[package]
name = "hum-matrix-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
uniffi = "0.24.3"
hum-matrix-core = { path = "../core" }

[build-dependencies]
uniffi_build = "0.24.3"
